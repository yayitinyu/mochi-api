services:
  mochi:
    build: .
    container_name: mochi-api
    ports:
      - '3000:3000'
    environment:
      # 日统计按此时区冻结日期边界，按需修改
      - TZ=Asia/Shanghai
    volumes:
      - ./data:/data
    restart: unless-stopped
